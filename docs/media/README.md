# Media assets

| File | Content |
| --- | --- |
| `preview.mp4` | Local copy of the overview clip |
| `preview-poster.jpg` | Optional poster frame |
| `preview.html` | Local Vimeo background player (optional) |

The README uses a native GitHub player via a `user-attachments` URL (uploaded once through the web editor).
